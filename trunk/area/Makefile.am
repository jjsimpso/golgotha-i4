## Process this file with automake to produce Makefile.in

include $(top_srcdir)/Makefile.am.global

noinst_LIBRARIES = librectlist.a

librectlist_a_SOURCES = $(RECTLIST_SRCS)

CLEANFILES = $(RECTLIST_CLEAN)

