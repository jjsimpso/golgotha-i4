## Process this file with automake to produce Makefile.in

include $(top_srcdir)/Makefile.am.global

noinst_LIBRARIES = libdevice.a libkeys.a

libdevice_a_SOURCES = $(DEVICE_SRCS)
libkeys_a_SOURCES = $(KEYS_SRCS)

CLEANFILES = $(DEVICE_CLEAN) $(KEYS_CLEAN)

